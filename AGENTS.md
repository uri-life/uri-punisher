# Repository Instructions

- Use Python 3.14 or newer for all development and validation work.
- Use `uv` for Python environment management and command execution.
- Use `ruff` for linting and formatting.
- Use `pytest` for tests.
- Add or remove dependencies only through `uv`; do not edit `pyproject.toml` directly for dependency changes.
